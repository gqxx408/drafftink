[package]
name = "format_enbx"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
drafftink-core = { path = "../../crates/drafftink-core" }
egui = "0.29"
log = "0.4"
zip = { version = "0.6", default-features = false, features = ["deflate"] }
quick-xml = "0.31"
