[package]
name = "drafftink-backend"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
drafftink-core = { path = "../drafftink-core" }
drafftink-etl = { path = "../drafftink-etl" }
axum.workspace = true
tokio.workspace = true
tower.workspace = true
tower-http.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
serde.workspace = true
serde_json.workspace = true
uuid.workspace = true
chrono.workspace = true
anyhow.workspace = true
sled.workspace = true
bincode.workspace = true
log.workspace = true
async-trait = "0.1"
jsonwebtoken = "9"
argon2 = "0.5"
# 录播 / 资源管理平台依赖（均为离线锁文件中已缓存的 crate）
# WebSocket 升级复用 axum 内建 ws（内部以 TokioIo 包装 hyper::Upgraded，
# 避免 Upgraded 未实现 tokio AsyncRead/Write 的编译问题），不再直接依赖
# hyper / tokio-tungstenite / sha1。
futures-util = "0.3"
base64 = "0.21"
sha2 = "0.10"
hex = "0.4"
