# 前端运行环境配置（Vite 仅暴露以 VITE_ 开头的变量）
# 后端默认监听 0.0.0.0:8080，且 CORS 允许任意来源，故可直接指向后端地址。
VITE_API_BASE=http://localhost:8080

# SM4 信封密钥派生所用的"校内共享密钥"（与后端 DRAFTTINK_JWT_SECRET 保持一致）。
# 这是"预置到内部应用的信封密钥"模式：移动端预置校园密钥，用于在本地解密消息正文。
# 若后端以自定义 DRAFTTINK_JWT_SECRET 启动，此处必须填相同值，否则消息正文无法解密。
VITE_SM4_SECRET=drafftink-backend-default-secret
