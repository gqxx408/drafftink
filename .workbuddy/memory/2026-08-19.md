# 2026-08-19

## 视频解码默认上限：4K 改回 1920（drafftink-desktop）
- 上轮（2026-08-18）把 `VideoPlayer::new()` 默认 `max_dim` 从 1920 提到 3840（4K），并同步 8 处注释。
- 本轮用户要求改回 1920。已将值 `3840.0 → 1920.0`（video_player.rs:912）及全部 8 处注释（模块头、PRELOAD_FRAMES 预算 33MB×6≈198MB→8MB×6≈48MB、doc「默认 3840」→「默认 1920」、new 便捷入口、核心降维说明、line 1780 测试注释）完整回退到 1920 基线。
- 残留「4K」仅出现在原始基线历史措辞「教学场景几乎不需要 4K」中，与默认值无关，保留。
- 验证：`cargo clippy -p drafftink-desktop --bin drafftink-desktop --tests` 改动 crate 0 警告。
- 结论：默认按 ≤1080p（最长边 1920）解码不缩放，>1920 才降维；单帧 ≤8MB，预加载 6 帧 ≤48MB。
