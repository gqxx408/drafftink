## 工具集：直尺/正多边形/函数绘图（并行会话 + 本会话协作）
- **并行会话已完成**：`RulerTool`/`PolygonTool` + `ActiveTool::Ruler/Polygon` + `ShapeKind::Polygon { center, radius, sides }`（带字段变体）+ `TeachingToolKind::Ruler/Polygon(n)`（edit 顶栏菜单）+ `shape_renderer::draw_polygon`（`polygon_vertex_count_matches_sides` 测试）+ `SelectedElement::Function` + `FunctionPlotInstance`（宿主层函数实例，user_rect/scale/expr/expr_str）+ `function_parser.rs`（**并行扩展了幂运算 `^` + strip_equation_prefix，10 个测试**）。
- **本会话交付**：
  - `function_parser.rs`（新建）：手写递归下降解析器（Const/Var/Add/Sub/Mul/Div/Sin/Cos/Tan + 幂运算 `^` 右结合）+ `parse`/`eval`/`sample_points`；**隐式乘法仅无空白紧邻生效**（`2x` 合法、`1 2` 报"多余内容"）；parse_errors_are_clear 等 6 个测试。与并行会话扩展融合成 10 个测试全过。
  - 函数绘图集成：`FunctionPlotTool`（tools.rs 坐标系+曲线绘制 `draw_function_plot`，±10 单位网格 + 轴 + 200 采样点）+ `ActiveTool::FunctionPlot` + `activate_function_plot` + `update_active_tool` 分支（egui Window 输入表达式实时预览 + Enter/按钮提交）+ `commit_function_plot`（提交为**宿主层 FunctionPlotInstance** + `UndoCmd::InsertFunction`，与并行删除/Undo 路径对称）。
  - 修复并行半成品：`undo.rs` 加 `InsertFunction`/`RemoveFunction` 变体 + apply_undo/apply_redo 分支 + `set_overlay_user_rect` 加 `Function` 分支；`FunctionPlotInstance` 改 `pub(crate)`。
  - edit 端 `TeachingToolKind::FunctionPlot` 变体 + label "📈 函数绘图 (F)" + 顶栏教具菜单项；desktop 消费 match 加分支。
  - 并行中间产物（`marquee_start`/`marquee_rect`/`function_menu`/`FunctionMenuState`/`FunctionPlotInstance.expr/expr_str`）加 `#[allow(dead_code)]` 保持 0 警告。
- **验证**：clippy --all-targets **0 警告**；`cargo test -p drafftink-desktop` **85 过 / 0 败**；`cargo test -p drafftink-enbx` 56+3 过。
- **未交付（报告为后续）**：底边栏重构（顶栏→底部 + 授课底边栏）、授课工具（聚光灯/幕布/倒计时，display crate 全新）、框选文本→函数绘图菜单接线（marquee 字段已就位未接线）。
- **关键经验**：并行会话活跃时（多个 agent 同时改文件），编译/测试结果可能因文件竞态瞬时失败（`parse_errors_are_clear` 单独跑过、全量跑挂），需重跑确认；`ShapeKind::Polygon` 带字段变体仍可 `Copy`（全字段 Copy），但穷举 match 需同步。

## 数轴教具（NumberLineTool）
- **core**：`ShapeKind::NumberLine { start: [f32;2], end: [f32;2], step: f32, label_interval: i32 }` 变体（Polygon 带字段先例；derive 无 Eq 已支持 f32）。渲染以 **rect 为唯一真相来源**：方向由 rect 宽高比派生（宽≥高 → 水平左→右、箭头在右；否则垂直上→下、箭头在底），`start`/`end` 仅作提交定义参数保存（与 Polygon 一致）。
- **shape_renderer**：`number_line_geometry` 纯函数（返回主线起终点 + 等距刻度偏移，不足一步截断）+ `draw_number_line`（主线 + 末端箭头 + 每 step px 短竖线 + 每 label_interval 个标数字）+ draw_shape 分支。
- **tools.rs**：`NumberLineTool { start, end, dragging, step: 20.0, label_interval: 2 }` + `ActiveTool::NumberLine` + `draw_number_line_tool` 预览（复用 number_line_geometry）。
- **app.rs**：`activate_number_line` + `update_active_tool` 分支（**一次性拖拽**：按下定起点 → 拖动定终点 → Shift 用 `snap_dir_grid45` 吸附水平/垂直 → 松开提交 `ShapeKind::NumberLine` + 包围盒 rect，走 `commit_shape_geom` → InsertShape Undo ✓）+ `shape_kind_to_enbx` 映射 "line" + tool_requested match。
- **edit**：`TeachingToolKind::NumberLine` + label "📏 数轴 (N)" + 顶栏教具菜单项 + `shape_kind_label` 分支。
- **测试**：`numberline_tick_count`（200px/step20 → 11 刻度等距；step30 → 7 刻度截断）+ `numberline_axis_direction`（水平主线在 center().y、垂直在 center().x）。
- **验证**：clippy 0 警告；desktop **90 过 / 0 败**（含 2 个新测试，不破坏既有 88）。
- **顺带修复并行残留**：`camera.screen_to_world` 签名从 `[f32;2]` 改为 `Pos2`（app.rs:1673 同步）；`ShapeDrawState.start_screen` 已改 `Pos2` 但 2533/2546 两处 `if let (Some(start), Some(cur))` 未同步（改 `if let Some(cur)` + 直接取 start）；`ShapeDrawState.kind` 未读 + `insert_shape_at` 未用（#[allow(dead_code)]）。

## 倒计时器（CountdownTool）+ 授课工具面板 + GitHub 提交
- **tools.rs**：`CountdownTool`（total/remaining/is_running/is_finished/position/input_text + last_tick/pending_press/dragging 交互态）+ `format_mmss` 纯函数 + `parse_input`（M:SS/纯秒，Err 不 panic）+ `tick`（每秒减 1）+ `flash_color`（到 0 红/白 500ms 闪烁、剩余≤10s 红）+ `rect()`（200×80）+ `draw_countdown`（半透明黑圆角背景 + 白边框 + 大号 monospace 数字 + 右上角 ✕）+ `ActiveTool::Countdown`。`draw_active_tool` 签名加 `time: f64` 参数（驱动闪烁，其余教具忽略）。
- **app.rs**：`activate_countdown`（画布中央）+ `update_active_tool` 倒计时分支（**输入阶段**：egui Window TextEdit 输入 → 确认 → parse_input 自动开始；**显示阶段**：画布绘制 + 点击数字开始/暂停/到时重开 + 拖拽移动（>4px 判拖拽 vs 点击）+ ✕ 关闭 + Instant 驱动每秒递减 + 运行中 200ms / 到时 500ms `request_repaint_after`）。
- **授课模式**：Teach 分支（F5）加 `update_active_tool(ctx)`（教具在授课模式可交互，不只读）+ `teach_tools_panel`（左上角「授课工具」窗口：⏱倒计时/📏直尺/📐三角尺/📐量角器）。
- **egui 0.29 坑**：`rect_stroke` 只 3 参（无 StrokeKind::Outside）；`draw_active_tool` 传 time 需改调用点。
- **测试**：`countdown_format_minutes_seconds`（format_mmss 0/59/330/5999）+ `countdown_finish_flashes`（tick 到 0 finished + 闪烁红白 + 最后 10s 红 + M:SS/纯秒/非法解析）。flash_color 用 `remaining_seconds <= 10 && > 0`（无 total 限制）。
- **验证**：clippy **0 警告**；desktop **92 过 / 0 败**。
- **GitHub**：commit `0cbf262` 推送到 `git@github.com:gqxx408/drafftink.git` main（`cf1e2d6..0cbf262`）。

## 放大镜（MagnifierTool，提示词 2）
- **app.rs**：`MagnifierTool { center, radius:120, zoom_factor:2.0, active }`（`Default`；**纯 UI 覆盖层**，不序列化、不进文档数据模型）；`IntegratedApp.magnifier` 字段 + `new()` 初始化。
- **UI**：左上角「授课工具」面板加第二行 `🔍 放大镜`（`selectable_label` 显示激活态，点击切换 on/off，Esc 退出）。
- **交互（`update_magnifier`）**：Esc 退出；圆心跟随 `pointer.hover_pos()`（**不用 response 拦截**，不吞画布交互）；鼠标滚轮 `smooth_scroll_delta.y * 0.004` 在 1.0–4.0 间调倍数；随后 `draw_magnifier`。
- **渲染（`draw_magnifier`）**：置于 Foreground 最上层（`update` 末尾、`update_active_tool` 之后、profiler 之前）。圈内 `with_clip_rect` 外接正方形裁剪，**只重绘当前页可见的宿主叠加元素**：形状（`draw_shape`，屏幕矩形经 `magnifier_transform` 放大后重绘）+ 函数绘图（`draw_function_plot`）。圈外压暗用 `annulus_mesh`（**手拼外圆-内圆环形三角网格**，egui 0.29 无圆形 clip / PathShape 无 even_odd）+ 3px 亮蓝圆圈边框 + 下方放大倍数角标。
- **变换**：`magnifier_transform(screen, center, canvas_offset, canvas_zoom, zoom_factor)` 纯函数——屏幕→画布→围绕圆心放大→画布→屏幕，`canvas_offset`/`canvas_zoom` 在折算中抵消，**等价于圆心基准纯屏幕缩放** `center + (screen-center)*zoom_factor`。
- **测试**：`magnifier_transform_correct`（恒等画布 2x 外扩 + 通用画布参数 3x 手算校验 + **任一 canvas 参数/倍数下圆心不动** 不变量）。
- **验证**：clippy 无 app.rs 警告；desktop **93 过 / 0 败**（含新测试）。
- **GitHub**：commit（待推）到 `git@github.com:gqxx408/drafftink.git` main，`0cbf262..`。
